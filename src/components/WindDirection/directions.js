const init = 315;
const value = 22.5;
export const directions = {
    N: init,
    NNE: init + value,
    NE: init + value * 2,
    ENE: init + value * 3,
    E: init + value * 4,
    ESE: init + value * 5,
    SE: init + value * 6,
    SSE: init + value * 7,
    S: init + value * 8,
    SSW: init + value * 9,
    SW: init + value * 10,
    WSW: init + value * 11,
    W: init + value * 12,
    WNW: init + value * 13,
    NW: init + value * 14,
    NNW: init + value * 15,
}